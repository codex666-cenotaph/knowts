Place your TLS certificate and key here if you use the "bring your own cert"
mode in the Caddyfile (tls /certs/knowts.crt /certs/knowts.key).

Actual .crt/.key files are gitignored — do not commit private keys.
